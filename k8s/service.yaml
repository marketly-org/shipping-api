apiVersion: v1
kind: Service
metadata:
  name: shipping-api
  namespace: marketly
  labels:
    app: shipping-api
    tier: api
spec:
  type: ClusterIP
  selector:
    app: shipping-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
